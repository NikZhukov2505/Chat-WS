import React, { FC } from 'react';
import Login from '../../components/Login/Login';

const Home: FC = () => {
    return (
        <div>
            <Login />
        </div>
    );
};

export default Home;